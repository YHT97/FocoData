spring:
    datasource:
        url: jdbc:postgresql://localhost:7000/db
        username: postgres
        password: postgres
        driver-class-name: org.postgresql.Driver
    jpa:
        database: POSTGRESQL
        show-sql: true
        hibernate:
            ddl-auto: update